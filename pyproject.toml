[project]
name = "demo-copilot"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "apo_huang", email = "apo_huang@cmoney.com.tw"},
]
dependencies = []
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
